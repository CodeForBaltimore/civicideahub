import React from 'react';

const StatelessTemplate = (props) => {
    return (
        <div>
            <h1>I don't have state so I am just a function</h1>
        </div>
    );
}

export default StatelessTemplate;
